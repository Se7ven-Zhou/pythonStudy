# coding:utf-8


import os

# getcwd() 获取当前路径
# path.realpath(__file__) 获取当前模块路径
# path.join(a,b) 连接两个路径成一个新的路径
# mkdir(路径) 新建文件夹
# rmdir(路径) 删除文件夹
# listdir() 获取当前路径下的目录列表
# path.isdir 判断当前文件是否是目录，返回bool
# path.isfile 判断当前文件是否是文件，返回bool
# path.split(路径) 拆分路径,拆分成前面路径和最后一个文件（最后一个是文件夹）
# path.exists(路径) 判断文件是否存在

"""当前目录生成一个文件夹，比如存放测试报告"""


# now_address = os.getcwd()
# new_address = os.path.join(now_address,"seven")
# if os.path.exists(new_address):  # 判断是否存在
#     print("已经存在！")
# else:
#     print("不存在，创建一个吧！")
#     os.mkdir(new_address) # 创建文件夹

